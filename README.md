# cloud-itonami-iso3166-gbr

Open ISO 3166 Blueprint for **GBR** — **`:implemented`**.

Market-entry compliance actor (`marketentry`) with flagship HARD check
`uk-entity-missing` and `vat-unverified`.

```
clojure -M:dev:test
clojure -M:dev:run
```

AGPL-3.0-or-later.
